// Criar um Array dos pares das cartas
const cartas = [1, 1, 2, 2, 3, 3, 4, 4]

// criar uma funçao assincrona para as img
async function geraImagensPar () {
    const imagemPar = {}
    for (let i =0; i < cartas.length; i++){
        if(!imagemPar[cartas[i]]) {
            const id = ''
        }
    }
}

